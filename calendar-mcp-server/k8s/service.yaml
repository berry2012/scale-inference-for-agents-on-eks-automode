apiVersion: v1
kind: Service
metadata:
  name: calendar-mcp-server
  labels:
    app: calendar-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: calendar-mcp-server
