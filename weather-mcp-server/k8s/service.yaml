apiVersion: v1
kind: Service
metadata:
  name: weather-mcp-server
  labels:
    app: weather-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: weather-mcp-server
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
